import 'package:flutter/material.dart';

class AppRecommendedList extends StatelessWidget {
  const AppRecommendedList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
