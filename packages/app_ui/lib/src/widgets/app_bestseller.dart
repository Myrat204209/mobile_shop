import 'package:flutter/material.dart';

class AppBestseller extends StatelessWidget {
  const AppBestseller({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
